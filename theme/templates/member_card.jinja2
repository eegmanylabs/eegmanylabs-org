{% import 'macros/icon_button.jinja2' as IconButton %}
{% from 'macros/utils.jinja2' import add_button %}

<sl-card id="{{ leaf.name }}" class="member-card {{ leaf.front['Tags']|join(" ") }}">
  {% if leaf.front['Image'] != "None" %}
    <img slot="image" src="{{ leaf.front['Image'] }}" alt="Portrait of {{ leaf.front['FullName'] }}">
  {% else %}
    {% set name_parts = leaf.front['FullName'].split(' ') %}
    {% set initials = name_parts[0][0] ~ name_parts[-1][0] %}
    <div slot="image" class="member-card__initials" aria-hidden="true"><span>{{ initials|upper }}</span></div>
  {% endif %}
  <div class="member-card__content">
    <h2>{{ leaf.front['FullName'] }}</h2>
    <p class="member-card__affiliation">{{ leaf.front['Affiliation'] }}{% if leaf.front['Country'] != "Unaffiliated" %}<span aria-hidden="true"> · </span><span>{{ leaf.front['Country'] }}</span>{% endif %}</p>
    <div class="tag-list" aria-label="Roles">
      {% for tag in leaf.front['Tags'] %}
        {{ add_button(tag) }}
      {% endfor %}
    </div>
  </div>
  {% if leaf.front['ScholarUrl'] != "None" or leaf.front['UniUrl'] != "None" or leaf.front['LabUrl'] != "None" or leaf.front['StudyId'] != "None" %}
    <div slot="footer" class="member-card__links">
      {{ IconButton.render(leaf.front['ScholarUrl'], "Google Scholar profile", "fab-google-scholar") }}
      {{ IconButton.render(leaf.front['UniUrl'], "University profile page", "fas-building-columns") }}
      {{ IconButton.render(leaf.front['LabUrl'], "Lab website", "fas-flask") }}
      {% if leaf.front['StudyId'] != "None" %}
        {% set replicationUrl = "/replications/#" ~ leaf.front['StudyId'] %}
        {{ IconButton.render(replicationUrl, "Replication page", "fas-clipboard", external = false) }}
      {% endif %}
    </div>
  {% endif %}
</sl-card>
